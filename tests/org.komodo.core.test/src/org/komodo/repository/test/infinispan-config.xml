<infinispan xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="urn:infinispan:config:5.10 http://www.infinispan.org/schemas/infinispan-config-5.2.xsd"
    xmlns="urn:infinispan:config:5.2">
    <!-- Global settings shared by all caches managed by this cache container. -->
    <global>
    </global>
    <!-- The default configuration template for caches. -->
    <default>
    </default>
    <!-- Individually named caches. -->
    <namedCache name="Test Repository">
        <transaction
                transactionManagerLookupClass="org.infinispan.transaction.lookup.GenericTransactionManagerLookup"
                transactionMode="TRANSACTIONAL"
                lockingMode="PESSIMISTIC"/>

        <loaders passivation="false" shared="false" preload="false">

            <loader class="org.infinispan.loaders.file.FileCacheStore"
                fetchPersistentState="true" ignoreModifications="false" purgeOnStartup="false">
                <properties>
                    <property name="location" value="${java.io.tmpdir}/TestLocalRepoPersistence"/>
               </properties>
            </loader>

        </loaders>
    </namedCache>
</infinispan>